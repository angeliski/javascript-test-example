function categoria(cursos, categoria) {
  //não lembro como faz, mas aqui eu preciso fazer um map para "capturar" apenas as categorias de cada elemento
}

function duracaoMaiorQue(cursos, duracao) {
  //não lembro como faz, mas aqui eu preciso fazer um map para "capturar" apenas a duração de cada elemento
  //depois eu faria um if para separar aqueles que possuem mais de 80 horas e torná-lo um array
}

function duracaoMenorQue(cursos, duracao) {
    //não lembro como faz, mas aqui eu preciso fazer um map para "capturar" apenas a duração de cada elemento
  //depois eu faria um if para separar aqueles que possuem menos de 80 horas e torná-lo um array
}

function comProfessor(cursos) {
  //não lembro como faz, mas aqui eu preciso fazer um map para "capturar" apenas o professor de cada elemento
}

function semProfessor(cursos) {
  //um map para capturar os professores e, caso o valor seja uma string vazia, eu retorno os cursos que não possuem professor
  
}

function descricao(cursos, regex) {
  //primeiro eu converto o elemento em uma string, depois eu faço um outro map para capturar a descrição de cada elemento
  
}

module.exports = {
  categoria,
  duracaoMaiorQue,
  duracaoMenorQue,
  comProfessor,
  semProfessor,
  descricao
};
